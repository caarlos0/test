brew:
  github: 
    owner: caarlos0
    name: homebrew-tap
  folder: Formula
  caveats: "Test formula, dont use it. It doesnt do anything"
archive:
  replacements:
    darwin: macOS
    amd64: 64bits
build:
  binary: test
  main: ./main.go
  goarch:
    - amd64
fpm:
  formats:
    - deb
