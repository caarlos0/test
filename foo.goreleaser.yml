brew:
  repo: caarlos0/homebrew-tap
  folder: Formula
  caveats: "Test formula, dont use it. It doesnt do anything"
archive:
  replacements:
    darwin: macOS
    amd64: 64bits
build:
  binary_name: foo
  main: ./foo/main.go
  goos:
    - darwin
  goarch:
    - amd64

